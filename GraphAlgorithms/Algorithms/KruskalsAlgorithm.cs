namespace GraphAlgorithms.Algorithms;

public partial class GraphAlgorithm
{
    public static List<Graph.Edge> Kruskals(in Graph graph, int startVertex)
    {
        var mst = new Graph();
        
        // Find minimum spanning tree
        // It's way to late right now to actually think so maybe leave it until tomorrow at least
        // I can't even remember what this one does anymore
        // I haven't even properly started Prims yet
        // AHHHHH

        return new List<Graph.Edge>();
    }
}
