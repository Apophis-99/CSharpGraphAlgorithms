namespace GraphAlgorithms.Algorithms;

public partial class GraphAlgorithm
{
    public static List<Graph.Edge> Prims(in Graph graph, int startVertex)
    {
        var mst = new Graph();
        
        // Mark the starting vertex as visited
        //  - Find minimum weight edge that connects a visited vertex to an unvisited vertex
        //  - Add this edge to MST
        //  - Mark unvisited vertex as visited
        // Return MST

        return new List<Graph.Edge>();
    }
}
